import { FormatCuisinesPipe } from './format-cuisines.pipe';

describe('FormatCuisinesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCuisinesPipe();
    expect(pipe).toBeTruthy();
  });
});
